// Адрес сайта-платформы Jalyk (личный кабинет пользователя). Скопировано из
// apps/web (BETTER_AUTH_URL — канонический адрес сайта); меняется вместе с ним.
// Студия встраивается в чужие приложения, поэтому отдельного источника адреса у
// неё нет — держим константой здесь.
export const JALYK_SITE_URL = 'http://192.168.1.192:3000'

// Страница со списком проектов в ЛК — куда отправляем пользователя, если проект
// по переданным projectId и ключу недоступен.
export const JALYK_PROJECTS_URL = `${JALYK_SITE_URL}/projects`

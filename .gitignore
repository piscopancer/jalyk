node_modules
.env
.env.*
!.env.example
!.env.*.example
.turbo
dist
test
_legacy/apps/server/prisma
_legacy/packages/trpc/src/prisma

# Сгенерированный клиент Prisma (создаётся `prisma generate`)
**/src/generated/
# Локальные базы SQLite
*.db
*.db-journal
# Кэш и сборка TanStack Start
.tanstack
.output
.nitro
# Дерево роутов, генерируется плагином TanStack
routeTree.gen.ts
# Локальное хранилище ассетов (тестовый драйвер storage)
apps/api/uploads/
# Логи отладки tsx watch
_watchtest.*

# Локальные TLS-сертификаты mkcert для dev (jalyk.local.dev)
certs/

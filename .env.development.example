# Скопируйте в .env.development. Переменные БД для локальной разработки;
# накладываются поверх общего .env. Выбирается процессом в режиме development
# (vite по mode, api — флагом --env-file в dev-скрипте).
DB_TARGET="local"
